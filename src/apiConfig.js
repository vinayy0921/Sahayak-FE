// src/apiConfig.js

// ⚠️ CHANGE THIS URL if your Backend folder name in htdocs is different
// If your backend folder is named "Sahayak-BE", keep it like this:
export const API_BASE_URL = "http://localhost/Sahayak-BE/api";

// If you are using a real server later, you only change this one line.